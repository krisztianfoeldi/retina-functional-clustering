# Development dependencies

# Remove them from this file in production, 
# then use renv::clean(actions = "unused.packages") and renv::snapshot()

library(lintr)
library(styler)
library(testthat)

# Production dependencies
